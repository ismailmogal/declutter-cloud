# Declutter Cloud Backend

**SECURITY WARNING:**

You MUST set a strong, random `SECRET_KEY` in `backend/.env` for production. Never use the default fallback. Example:

```
SECRET_KEY=REPLACE_WITH_A_LONG_RANDOM_SECRET_KEY
``` 